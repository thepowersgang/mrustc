/**
 * Binding header for the macro_rules syntax extension
 */
#pragma once

#include <synext.hpp>

namespace AST {
    class Expr;
    class Module;
}
class TokenTree;
class TokenStream;
class MacroRules;
