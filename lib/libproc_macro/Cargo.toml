[package]
name = "proc_macro"
version = "0.0.0"

# - Populate features so the crate signature is different to rustc's
[features]
default = ["mrustc"]
mrustc = []

[lib]
path = "src/lib.rs"

[[bin]]
name = "dump"
path = "tools/dump.rs"